- name: expense 
  hosts: mysql
  become: yes
  tasks:
  - name: install mysql
    ansible.builtin.package:
      name: mysql-server
      state: installed
  
  - name: start mysql
    ansible.builtin.service:
      name: mysqld
      state: started
      enabled: yes